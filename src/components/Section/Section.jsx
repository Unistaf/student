import React from "react"

const Section = ({ children }) => {
    return (
        <div className="py-24 px-3 max-w-7xl mx-auto">
            {children}
        </div>
    )
}

export default Section